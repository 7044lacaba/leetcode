
class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        copy = nums
        for i, item in enumerate(copy):
            new_list = 
            if 
        